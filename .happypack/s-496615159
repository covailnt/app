'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _firebase = require('./firebase');

var _firebase2 = _interopRequireDefault(_firebase);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var initialized = false;
var handler = function handler(store, action) {
  var state = store.getState();
  var currentProject = state.currentProject;

  if (action.type == 'SET_CURRENT_PROJECT' || currentProject && !initialized) {
    initialized = true;
    var id = store.getState().currentProject.id;
    var ref = _firebase2.default.database().ref('resources');
    ref.orderByChild('projectId').equalTo(id).on('value', function (_v) {
      var value = [];
      _v.forEach(function (sn) {
        value.push(_extends({}, sn.val(), { id: sn.key }));
      });

      store.dispatch({
        type: 'SET_RESOURCES',
        value: value.sort(function (a, b) {
          return a.date > b.date;
        })
      });
    });
  }
};

exports.default = handler;